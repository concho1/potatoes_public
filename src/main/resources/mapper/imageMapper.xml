<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.goott.potatoes.common.mapper.ImageMapper">

    <insert id="insertImage" parameterType="Image">
        INSERT INTO image (img_key, created_at, url)
        VALUES (#{imgKey}, #{createdAt}, #{url})
    </insert>


    <select id="selectImageByKey" parameterType="String" resultType="Image">
        SELECT img_key, created_at, url
        FROM image
        WHERE img_key = #{imgKey}
    </select>

    <update id="updateImage" parameterType="Image">
        UPDATE image
        SET created_at = #{createdAt},
            url = #{url}
        WHERE img_key = #{imgKey}
    </update>

    <delete id="deleteImage" parameterType="String">
        DELETE FROM image
        WHERE img_key = #{imgKey}
    </delete>

</mapper>
